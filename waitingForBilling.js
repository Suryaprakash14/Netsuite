/**
 * @NApiVersion 2.x
 * @NScriptType MapReduceScript
 * @NModuleScope SameAccount
 */
define([],

function() {
   
	 /**
     * Version 2.0
     * Date   01 FEB 2022
     * Author Suryaprakash Ragavan
     * 
     * Script Description : "* Create saved search on Sales order record, retrieve SO's which are pending for billing.
* Send one email for one customer with SO's which are pending billing. "
     **/
    function getInputData() {

    }

    function map(context) {

    }

    function reduce(context) {

    }


    function summarize(summary) {

    }

    return {
        getInputData: getInputData,
        map: map,
        reduce: reduce,
        summarize: summarize
    };
    
});
